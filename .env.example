# The generator reads the last non-empty line as the camera password.
# Keep this file outside Git; runtime/ is already ignored.
CAMERA_PASSWORD_FILE=runtime/camera-password.txt

# Optional, non-secret overrides.
CAMERA_USERNAME=admin
# VITE_HLS_ORIGIN=http://192.168.0.119:8888
